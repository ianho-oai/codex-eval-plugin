#!/usr/bin/env python3
"""Repository convenience entrypoint; the plugin also runs standalone."""
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent / 'plugins' / 'codex-eval-plugin'))
from ceval.cli import main
raise SystemExit(main())
